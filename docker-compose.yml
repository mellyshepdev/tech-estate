services:
  tech-estate:
    image: nginx:1.27-alpine
    container_name: tech-estate
    ports:
      - "8090:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    restart: unless-stopped
